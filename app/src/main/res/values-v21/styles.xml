<resources>



    <style name="AppTheme.ToolbarPopupTheme" parent="ThemeOverlay.AppCompat.Light">
        <!-- 设置Menu菜单的背景色，这里是个选择器，按下背景置灰 -->
       <!-- <item name="android:itemBackground">@drawable/item_pressed_selector</item>-->
        <!-- 设置Menu菜单的字体颜色 -->
        <item name="android:textColorPrimary">@android:color/black</item>
        <!-- 设置Menu窗口不覆盖Toolbar视图 -->
        <item name="overlapAnchor">false</item>
    </style>

    <!--透明背景-->
<!--    <style name="AppTheme.TranslucentStatus">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>-->

    <style name="AppTheme.TranslucentStatus">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>


    <style name="AppTheme.HomepageWidgetColor">
        <!--scroll边界阴影色-->
        <item name="colorPrimary">@color/colorHomepagePrimary</item>

    </style>

</resources>
